object ExecuteNTimes {
  //def executeNTimes(f: () = Unit, n: Int): Unit =
    //for(i <- 1 to n) do f() while(n < 5)

  def helloWorld(): Unit = println("Hello, world")

  def main(args: Array[String]): Unit ={
    //executeNTimes(helloWorld, 3)
  }
}
